name: Update README cards

on:
  schedule:
    - cron: "0 3 * * *" # Runs every day at 3:00 AM UTC
  workflow_dispatch: # Allows manual trigger from GitHub UI

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Generate stats card (dark)
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=phowdecayed&show_icons=true&include_all_commits=true&theme=dark&bg_color=00000000&hide_border=true&title_color=EEEEEE&icon_color=EEEEEE&text_color=888888
          path: profile/stats-dark.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate stats card (light)
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=phowdecayed&show_icons=true&include_all_commits=true&theme=light&bg_color=00000000&hide_border=true&title_color=1E1E1E&icon_color=1E1E1E&text_color=555555
          path: profile/stats-light.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit cards
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add profile/*.svg || true
          git commit -m "chore: update profile stats cards (light/dark)" || exit 0
          git push
